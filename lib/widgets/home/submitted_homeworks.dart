import 'package:flutter/material.dart';

class SubmittedHomeworks extends StatelessWidget {
  const SubmittedHomeworks({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Odovzdané úlohy'),
    );
  }
}
